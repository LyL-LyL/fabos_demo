server:
  port: 10001         #Eureka端口号

#项目名
spring:
  application:
    name: REGISTER


eureka:
  instance:
    hostname: localhost

  client:
    service-url:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
    fetch-registry: false    #不从其他注册中心获取数据
    register-with-eureka: false  #不在其他注册中心注册



